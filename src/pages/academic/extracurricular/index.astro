---
import Layout from '@/layouts/Layout.astro';
import ActivitySection from '@/components/extracurricular/ActivitySection.astro';
import activities from '@/data/extracurricular.json';
import { Separator } from '@/components/ui/separator';
---

<Layout>
  <main class="w-full flex flex-col">
    <section class="bg-secondary/50 pt-28 pb-20 text-center">
        <div class="container mx-auto px-4">
            <h1 class="text-4xl md:text-5xl font-bold tracking-tight">Kegiatan Ekstrakurikuler</h1>
            <p class="mt-4 text-lg max-w-3xl mx-auto text-muted-foreground">
                Mengembangkan minat, bakat, dan karakter santri di luar jam pelajaran formal melalui berbagai kegiatan yang positif dan mendidik.
            </p>
        </div>
    </section>

    {activities.map((activity, index) => {
      return (
        <>
          <section>
            <ActivitySection
              title={activity.title}
              description={activity.description}
              images={activity.images}
              reverse={index % 2 !== 0}
            />
          </section>
          {index < activities.length - 1 && <Separator />}
        </>
      )
    })}
  </main>
</Layout>
